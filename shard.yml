name: marten_profile_log
version: 0.1.1

authors:
  - Stephen Ierodiaconou <stevegeek@gmail.com>

crystal: '>= 1.20.1'

license: MIT

dependencies:
  marten:
    github: martenframework/marten
    # Track main (no version tags upstream); consumers pin a commit via override if needed.
    branch: main

development_dependencies:
  sqlite3:
    github: crystal-lang/crystal-sqlite3
